{
  "name": "my-warranty",
  "version": "0.1.0",
  "private": true,
  "description": "MY Warranty — mobile app, data operations console and shared domain logic.",
  "workspaces": [
    "packages/domain",
    "apps/admin"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "overridesNote": "exceljs pins uuid ^8, which has an unpatched buffer bounds check (GHSA-w5hq-g745-h8pq). v11 keeps the named v4 export exceljs uses; the XLSX parsing test covers it.",
  "overrides": {
    "uuid": "^11.1.1"
  }
}
